// This mock allows using the enzyme 'mount' to integration test your components without failing on Redux integration.
// To use this, just do `jest.mock('react-redux');` in your test.js file.
module.exports = {
    connect: () => (
        ReactComponent
    ) => ReactComponent
};